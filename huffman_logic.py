# huffman_logic.py - TO BE COMPLETED 

def build_huffman_lengths(freqs, limit):
    """
    Implement Huffman tree here.
    Input: List of frequencies.
    Output: List of bit-lengths (e.g., [3, 3, 2...])
    """
    # Return a dummy list of 0s for now so the code runs
    return [0] * limit

def get_canonical_map(lengths):
    """
    Implement Canonical Code generation (Section 4.4).
    Input: List of bit-lengths.
    Output: Dictionary mapping symbol -> binary string (e.g., {97: '110'})
    """
    # Return an empty dict for now
    return {}